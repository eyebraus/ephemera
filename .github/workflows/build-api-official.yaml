name: build-api-official

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout source
        uses: actions/checkout@v4
      - name: Build API Docker image
        run: docker build . --file api.dockerfile --tag ephemera.api:$(date +%s)

on:
  push:
    branches:
      - main
